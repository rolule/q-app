import type {
  ComponentSingleStyleConfig,
  SystemStyleObject,
} from '@chakra-ui/react'

const baseStyle: SystemStyleObject = {
  color: 'white',
}

export const Text: ComponentSingleStyleConfig = { baseStyle }
